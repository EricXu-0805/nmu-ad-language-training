[project]
name = "nmu-language-training-platform"
version = "0.0.1"
description = "南医大 · 轻中度痴呆 AI 语言沟通训练系统（本地部署，不上云）"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
