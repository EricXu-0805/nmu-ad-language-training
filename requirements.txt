# 后端依赖（本地部署，不上云）。阶段0 核心逻辑仅用标准库即可跑测试；
# 下列依赖在接 API/DB（阶段0 收尾→阶段1）时安装。
fastapi>=0.115
uvicorn[standard]>=0.30
sqlmodel>=0.0.22
pydantic>=2.7
python-multipart>=0.0.9
pytest>=8.0
alembic>=1.13          # 数据库迁移(真机患者数据禁止删库重建)

# 本地转录（阶段1）：ASR 模型选型待机构 GPU 配置确认（决策19），接口按可替换设计，
# 不写死模型；候选如 FunASR / Paraformer，本地运行、音频不出机构。禁止云端转录。

# 本地神经 TTS(小语的声音,CPU 实时;云端语音 API 属禁区——话术是未申报专利的研究 IP)。
# 装后下载音色: python -m piper.download_voices --data-dir data/tts zh_CN-huayan-medium
# 未装/无模型自动降级浏览器系统语音,部署机可选。
piper-tts>=1.4
