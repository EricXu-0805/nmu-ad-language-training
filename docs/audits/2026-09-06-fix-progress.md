# 2026-09-06 综合审计修复与验收

本轮由 Eric 明确授权 Codex 实施。审计基线 `3dbf0538f906fefebdf91ff8772aab7556f2fc66`，修复分支 `codex/full-audit-fixes-20260906`。原审计报告保存在项目父目录 `审核报告_20260906/`，保留当时事实，不用修复后的状态覆盖历史。

## 2026-09-07 当前阶段

[PR #1](https://github.com/EricXu-0805/nmu-ad-language-training/pull/1) 已合并，应用发布提交为 `5684a3a329b7189560942d6c2ec6a5f6444474bf`。[该提交的主分支 CI 34091089034](https://github.com/EricXu-0805/nmu-ad-language-training/actions/runs/34091089034) 五项全部成功。两种 Python 的后端分别为 3352 通过、21 跳过、0 失败，按完整进度记录独立重建，不合并环境计数；这些云端跳过项的本机定向补测为 21 通过、0 跳过、0 失败，不能反写为云端已执行。前端门通过，但云日志裁剪后未保留可独立提取的测试总数。

应用已安装到现役 systemd 裸机，目标机完整发布清单对照同一应用提交及独立网页产物通过：813 个源码文件、16 个网页发布文件。候选副本已迁移至 `e2a6d8f0b419`，旧完整树、旧数据和旧头备份保留；私有健康检查为 200，匿名受保护路由为 401。新头备份已在 VPS 完整校验。

**发布收尾尚未完成：公网入口仍为 503，新头异地传输等待明确授权，六个 VPS 定时器与 Mac 异地调度均暂停。** 正常入口开放后的浏览器检查尚未执行。具体维护与备份收据在父目录 `审核报告_20260906/` 的本轮受控审计记录中保存，公开文档不登记业务对象数量、标识或私有审计锚点摘要。

A17 的记账规则区分实际应用 SHA 与文档提交：后续状态文档登记 `5684a3a` 的事实，不把原 CI 或产物重标为文档提交，也不因单纯记账再部署应用。当前状态索引见 [部署状态](../RELEASE_STATE.md)；只有剩余发布步骤真实完成后才能补记完成，正式使用批准仍单独判断。

## 范围与完成标准

关闭确认的 18 项问题，以原失败场景的正向防回归、受影响模块回归和同一候选树的整体验证为准。测试通过只证明对应环境和场景，不代表所有可能故障均已排除。内部演练范围保持不变，正式评估和临床内容未签署时继续阻断；不使用真实受试者数据、真实录音或付费云调用作开发验收。

代码、本地测试、GitHub CI、托管规则、实际部署、目标设备、真实使用批准分别留证。候选改动不能记成已上线。

## 分批计划

1. 后端治理与证据：A01/A02/A08/A09/A10；处理与前端录音代次和播放回执的合同依赖。
2. 前端安全与输入：A03/A04/A05/A11/A12/A13/A16/A18；合成媒体测试加浏览器操作。
3. 运维和候选内容：A06/A07/A14；真实启动恢复验证、完整发布清单、候选内容隔离检查。
4. 发布及交接：A15/A17；托管规则回读，历史和当前状态分开，标清现役裸机与目标镜像拓扑。
5. 独立复核与全量验收：先聚焦测试，稳定后一次整套后端/前端/构建，发现新失败再修复重验。

## 问题状态

| 编号 | 优先级 | 问题 | 当前状态 |
| --- | --- | --- | --- |
| A01 | P1 | 量表 AI 草稿未完整验证云授权与撤销互斥 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A02 | P1 | 量表保存与锁定竞态导致锁定内容和总分不一致 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A03 | P1 | 开始请求等待时暂停被禁用 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A04 | P1 | 按估算等待朗读而提前开麦 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A05 | P1 | 旧录音回执影响新一轮录音 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A06 | P1 | 恢复演练未真正执行应用生命周期 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A07 | P1 | 发布树校验不完整、单向比较可能假绿 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A08 | P2 | 暂停时仍可处理、停止后的迟到 ASR 可落账 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A09 | P2 | 相同原始录音的并发请求重复落账 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A10 | P2 | 进云前未复核音频真实字节与收据 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A11 | P2 | 教育年限输入会把小数或负数静默改写 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A12 | P2 | 退出保护无法用键盘完成 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A13 | P2 | 开发代理缺少照护、研究与回应库路径 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A14 | P2 | PI 候选内容检查误用仓库默认目录 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A15 | P2 | main 没有启用必需检查 | 已应用并独立回读，见 [托管验收](2026-09-06-branch-protection.md) |
| A16 | P2 | 设备检查页标题被横向布局压缩 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |
| A17 | P2 | 交接文档版本、PIN 和部署拓扑互相矛盾 | 记账规则与当前维护状态复核通过；发布收尾仍待完成 |
| A18 | P2 | 照护员暂停和求助在小屏折叠线以下 | 已实现，针对性回归及主分支 CI 通过；维护窗口收尾中 |

## 外部验收与运行证据缺口

- 正式量表定义/协议/工作流政策与 PI 定义仍未交付；不得编造来解锁。
- 第 1 周回应库及第 2–8 周内容的书面签署不能由工程代理代签。
- 目标真机、房间、弱网、麦克风、长时间真实语音场景仍需现场留证。
- 240 场机械容量回执使用合成小音频，不代表真实语音并发容量。
- 本次已在获授权的维护窗口执行停写备份、隔离恢复、候选副本迁移及旧完整树保留；新头异地验收和发布收尾仍待完成，旧历史快照的清理不随本次操作自动获准。
- 护理员现场通知对象、响应时限、恢复与交班权限需院方决定。
- 研究披露阈值、去标识密钥治理和数据分区需具名责任人核验现役配置。

## 交叉复核追加关闭

- 呈现请求短暂为空时保留已确认的机器人身份，避免尚未播放完便误开麦；覆盖真实呈现 hook 到录音授权链。
- 回应请求开始时捕获播放代次；离开再返回同问也不能让迟到的旧回包覆盖重播内容。
- 恢复副本再次对照复制前固定的源清单，并执行匹配版本的完整验证；源验证后音频改变不能获得成功启动回执。
- 历史校验器按已核验指纹的固定字节执行，原归档路径被替换不能改变本次执行代码。
- 在经典滚动条占宽的 320px 浏览器视口内取消全局最小宽度导致的横向溢出。

## 历史验证记录（按当时候选保留）

以下记录描述各次验证发生时的版本与范围，其中“正在”“最终”“仍待”等表述不覆盖上方 2026-09-07 当前阶段。尤其 `7e8e4b7` 的测试和初次 CI 结果不能代替应用提交 `5684a3a` 的证据。

- 托管保护：已实际设置并独立 GET 回读，A15 关闭；见独立验收记录。
- 依赖共享门：退出 0；SBOM 对账、135 个包的当前 OSV 检查和隔离 Python 3.12.13 安装锁对账通过。已有一项明确不适用 RSC 路径的 React Router 豁免；镜像与系统包需 CI image 检查，此门不覆盖。
- 交付说明与部署合同：首轮 31 项通过；后续同范围文档继续修订，最终全量回归将重新验证。
- 候选 PI 内容检查：预期退出 1，`awaiting_pi_definition`、92 项缺口、创建/自动计分均关闭。该结果是门禁正确阻断，不能标作量表内容就绪。
- 前端最终完整检查：预检 80/80、主测试 1300/1300，失败和跳过均为 0；两批有重复覆盖，不相加冒充独立测试数。新增行为回归 31 项。lint 0 错误/警告，构建成功；保留原有无效动态导入的构建提示，不影响构建。
- 根复核：全仓 ruff 与差异空白检查通过。1024×600 / 320×600 本地浏览器实测设备页标题、照护安全操作固定位置，以及建档/编辑的非法整数拒绝。真实提交仅使用隔离合成账号；暂停与求助登记完成，界面明确提示求助不会自动通知别人。
- 后端首轮整套发现旧估时导出只被自测调用，已移除停用函数及两项旧自测，保留 31 项实际安全回归；固定提交后重跑后端整套。
- 初次公开推送曾因缺少明确上传授权被自动审批拒绝。Eric 随后明确批准公开上传及检查通过后的内部演练上线；`7e8e4b7` 已上传并建立 PR #1。五项 CI 中镜像扫描新发现基座 libuuid 的已修复高风险项，正在作精确包版本修复，未放松检查或新增豁免。
- `7e8e4b7` 本地后端最终全量 3288 项通过，Python 3.12 聚焦 91 项通过；该版本对应云端后端仍独立验收。镜像补修后的候选还须重新取得五项 CI 和生产恢复证据。最终事实保存在父目录 `审核报告_20260906/修复-最终验收报告.md` 和对应 evidence，不能把此候选文档当作已上线回执。

迁移与版本切换要求见 [候选迁移与上线前核验](2026-09-06-migration-and-release.md)。A17 同时纠正了 README/部署手册中已过期的第 1 周进云问位描述和自动化通道状态；没有改变内容签署或真实使用批准。
