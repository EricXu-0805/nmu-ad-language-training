# 本机自动检查回执（2026-08-12）

## 结论

本轮相关本机自动检查全部通过。这个结论只覆盖代码、模拟接口和网页端状态机；不覆盖真浏览器、真麦克风、云端语音、完整训练内容、医院或正式研究使用。

## 基线与边界

- 分支：`feature/phase1-flow-and-evidence-20260727`
- HEAD：`c77069dad0a2c836dfdedaf66bfab923cff855dc`
- 数据：未读取或输出 `platform/data/**` 中的文件内容。
- 网络与服务：未启动后端、前端或数据库长驻服务；未访问云端 provider、部署或生产。
- Git：未 commit、未 push、未 deploy、未暂存。

流程偏差说明：在历史 Claude 五文件修复完成后的只读审查批次中，一个子任务开始时使用过广域 `AGENTS.md` 查找，报告它可能触及 `data` 目录的元数据；没有打开、读取或输出其中的文件内容。随后全部检查均限定在明确路径。按最严格的“连目录元数据也不触碰”口径，这是一项已记录的流程偏差，不应归到 Claude 五文件执行批次。

## 后端

运行范围：

```text
tests/test_autopilot.py
tests/test_autopilot_api.py
tests/test_autopilot_contract.py
tests/test_autopilot_data_layer.py
tests/test_autopilot_plan_profile_migration.py
tests/test_autopilot_plan_profiles.py
tests/test_autopilot_positions.py
tests/test_autopilot_repeat.py
tests/test_autopilot_service.py
tests/test_repeat_intent_protocol.py
tests/test_tts_autopilot_strict.py
```

结果：**572 项通过，0 项失败。**

最终完成时间：`2026-08-12 19:59 CST`。执行命令：

```text
DATABASE_URL=sqlite:///:memory: PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider -q --tb=short -W ignore::DeprecationWarning tests/test_autopilot.py tests/test_autopilot_api.py tests/test_autopilot_contract.py tests/test_autopilot_data_layer.py tests/test_autopilot_plan_profile_migration.py tests/test_autopilot_plan_profiles.py tests/test_autopilot_positions.py tests/test_autopilot_repeat.py tests/test_autopilot_service.py tests/test_repeat_intent_protocol.py tests/test_tts_autopilot_strict.py
```

其中新增 9 条连续接口场景：

- 空转写、接近答案、未答出三种首轮分支，均连续走完三轮并告知答案，没有第四次录音。
- 第一级或第二级提示后答对，反馈确认前不推进，确认后只进入冻结计划中的下一题；重复确认不重复创建命令。
- 第一级或第二级提示后的识别降级/异常均暂停，不消耗下一提示，也不重复记账。

唯一提示为测试框架关于未来 `httpx2` 迁移的弃用警告，不是本轮产品行为失败。

## 网页端

- `npm test` 预检查：**31 项通过，0 项失败。**
- `npm test` 主测试：**969 项通过，0 项失败。**
- `npm run lint`：通过。
- `npm run build`：通过。

新增 6 项直接运行生产播放状态机的测试，覆盖：真正开始播放后才上报开始、真正结束后才上报结束、过早结束无效、播放/解码失败清理、取消 exact-once、中止下载后迟到音频不得播放。

构建有一个非阻塞提示：同一浏览器媒体依赖同时存在静态和动态导入，因此动态导入不会单独拆包。构建成功；该提示不等于真浏览器行为已经验收。

## 静态检查

- `git diff --check`：通过。
- 当前修改均为本机未提交差异；权威验收材料已放入 `docs/acceptance/2026-08-12/`。

## 仍然未通过

1. 目标浏览器与真实麦克风、扬声器的完整旅程。
2. 仅使用合成、无敏感内容的真实云端 TTS/ASR 验收。
3. 当前默认第 2 周内容的 60 个交付缺口。
4. 伦理、知情同意、权限、备份恢复及医院/正式研究验收。
