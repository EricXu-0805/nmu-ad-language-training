# 钱凯六项反馈的第二轮检查与修复

**最终状态（2026-09-24）：** 本文修复已随应用 `ce314df` 上线，10:38:05 UTC 重新开放；实际发布回执见文末及 [部署状态记录](RELEASE_STATE.md)。下文保留各阶段当时的事实；真实老人、平板和弱网验收仍未完成，首页试验版限制继续保留。

日期：2026-09-24。检查起点：本地 `e727a3a`，工作树起初干净；对照收据 260、262 与当前源码。下文按阶段保留当时的验证边界；最新状态见文末，线上版本以 `RELEASE_STATE.md` 和部署树校验回执为准。

## 六项反馈逐项结论

| 现场问题 | 已有能力与本轮处理 | 仍然存在的限制 |
|---|---|---|
| 老人抢答 | 增加“现在回答”：提问／提示播放时可主动停播，服务端确认同一道题的录音授权后进入作答。保留原提示级别，并单独记录提示被打断的事实。 | 这不是靠声音自动打断。点击前说出的内容不会追录；须等屏幕显示正在听再回答。自动声音打断仍需回声处理与真机验证。 |
| 超时后才答对 | 暂停后可选择“老人已答对／超时后才答对”。本轮修复反馈或告知答案播放期间暂停时，裁定误指向下一题的问题。 | 若已推进至下一题，不能把对上一题的观察直接裁给新题；已完成题仍走事后复核。没有录音证据时不能伪造一次回答。 |
| 答对但识别错误 | 已有“老人已答对”和“跳过本题”。本轮确认框明确题号、环节，并绑定打开时的状态，状态改变须重新核对。 | “已答对”只结束当前环节，“跳过本题”结束本题剩余环节；两者都不覆盖原始 AI 判定或自动锁定研究评分。 |
| 网络问题 | 已有请求重试。本轮把音频下载断流、正文悬挂、授权复核暂时失败一起纳入有限重试，超时后的旧音频不再参与播放。 | 真实弱网、平板系统中断与回答后等待时间仍需现场测量，不能把本地模拟通过当作网络问题全部解决。 |
| 从指定题开始／中断续练 | 新场次可由研究者选择冻结计划中的起始题，预览跳过范围、填写原因并确认。已保存录音在判分途中暂停后可继续处理；迟到的旧任务不能改写恢复后的结果。 | 指定起始题只用于没有训练证据的新场次。已经落为终态技术失败的回答仍走人工处理，不抹掉失败记录重跑。 |
| 后台逐题编号 | 已有冻结计划总序号、类型内序号及导出题号。本轮补上研究数据页“研究者现场裁定”入口，未录音便跳过的题可以按题号查到。 | 对账时需要同时看环节明细和现场裁定；没有录音的跳过题不会伪造环节明细。 |

## 本轮额外发现并修复

1. **反馈时裁定错题。** 当前题结论已落账、反馈语音未结束时暂停，旧代码以“首个未完成题”作为裁定目标，会误跳下一道尚未展示的题。现在以安全暂停命令锁定正在显示的题位，再寻找实际恢复位置。
2. **旧确认框可能提交到新回答。** 弹窗打开后另一页面推进，旧前端会取得新版本并自动重试旧决定。现在绑定题位及完整状态版本；异题、异环节、同题的新回答都要求重新确认。
3. **部分多要素题可能变成满分。** 做完并锁定一个环节，其余跳过时，原汇总只拿存在的环节作分母。现在含明确跳过环节的题排除出题级综合分，逐环节记录保留。收尾页显示实际排除原因，不再一律误报“未锁定”。正常流程的未完成题还受完成与锁分门禁保护。
4. **跳过题在研究页面不可见。** 后端已有裁定数据集，前端入口未同步。现已补上，并说明没有录音的跳过题去哪里核对。
5. **下载版本可能错标。** 屏幕停在旧版，后台发布新版后，原下载可能获得新版内容却沿用旧版文件名。研究 CSV 现在携带预期版本；不一致返回明确提示，刷新核对后再导出。
6. **系统暂停音频被当成老人静音。** 音频上下文停止运行时不再把零值或旧采样作为静音证据，恢复后重新检测开口。
7. **卡顿把短噪声放大成一句话。** 有声时长累计与开口检测采用同样的单帧上限，避免两帧迟到噪声越过短回答保护。
8. **重试只保护响应头。** 现在覆盖完整音频下载和两次授权复核；权限变化、无效音频不会因为重试被放行。
9. **备注错误被静默丢弃。** 本地备注校验失败时保留可编辑的确认框；同一提交标识携带不同备注时，后端拒绝把它当成成功重放。

以上第一批改动没有修改 14 秒作答上限、3／4.5 秒静音阈值、临床题目、单次回答的评分规则或既有研究锁分记录，也不涉及数据库迁移。下面继续实施的提前回答功能新增了一项数据库迁移。综合分仅修正含跳过环节的题被误当成完整题的处理。

## 继续实施：指定题位、处理中恢复和提前回答

1. **指定起始题。** 冻结计划题号与起始位置一一对应，前面的每个环节都有“跳过”回执、操作者、原因及可选备注。不会生成虚假的录音、回答或得分。同一启动请求的重试不得换题或改理由；已有训练证据不能重新选择起点。编辑起始题时暂停床旁自动启动，取消确认或选回第一题也不会意外开练，须明确重置才恢复自动启动。
2. **判分中暂停后继续。** 使用原来已收口的录音和回答位置继续识别／判分，不重新开麦。如果转写已完成，接着判分。新处理轮次与旧请求隔离；旧请求成功或报错晚到，都不能覆盖新结果或再次把场次暂停。连续服务异常各自保留失败事实，质量页按可证明的恢复轮次计数。
3. **“现在回答”。** 只对提问和提示开放，反馈／告知答案不开放。先实际停止声音，再保存独立的打断回执，最后取得录音授权；不会把停止播放冒充成播放完成。提示部分播放仍保留其原提示级别，导出可区分完整播放与提前回答、查看播放时长，供研究人员复核提示暴露。新增迁移 `a7c3e9d2b641` 允许打断回执；有该类事实后禁止回退删除其契约。
4. **服务不可用时保存现场决定。** AI 服务不就绪时，研究者仍可保存“已答对”或“跳过”，场次留在安全暂停，不发送新的播报或录音命令。服务恢复后重试原请求也不会偷偷重启 AI，需要明确点击继续。原有同意、撤回、录音和云处理授权门禁保留；没有录音仍不能凭空生成“回答正确”的录音证据。

提前回答的逐次导出在末尾增加 `prompt_playback_outcome`、`prompt_played_ms`、`prompt_exposure_needs_review`。播放时长取新打断回执的浏览器播放进度，不能推断老人听到了哪些词；旧的完整播放计时不会混入该列。历史记录证明不足时标为 `unverified` 并提示复核，不因新增展示列而拒绝整包导出。新打断提示所对应的回答自动标记 `ai_needs_review`，原评分与提示级别保留；已有研究冻结快照不改形状。

### 现场使用入口

- **从后面的题开始：** 新建场次后，在研究者训练台的 AI 带练卡片选择起始题，核对将跳过的题与环节，填写原因，再确认启动。照护员账号不能更改起始题。
- **判分途中停下后继续：** 等设备完成安全暂停，在训练台点击“继续 AI 自动带练”。已有录音接着处理，不要求老人重新回答。
- **提前回答：** 老人画面播报题目或提示时点“现在回答”，等“正在听您说”出现后再说。
- **AI 服务出故障：** 完成安全暂停后记录现场裁定。看到“现场决定已保存”即可确认已落账，后续选择继续 AI 或转人工。

### 仍需现场验证或另行确认

- 自动靠老人说话打断播报尚未实施；当前是明确点击的提前回答入口。
- 真实平板弱网、机器人回声、弱声、长找词停顿，以及停播到真正收音的时差，需要现场测量。作答时限和静音阈值未改变。
- 已保存但未完成的回答可以恢复；终态技术失败没有被改写成可重复判分的普通回答。
- 对照组内容、临床训练方案和评分规则未改动。

钱凯提到的对照组方案，本轮没有收到新的具体方案内容，因此没有自行改动对照组训练内容或研究设计。

## 验证范围

本轮只使用临时数据库、合成题目／音频和虚构账号验证。自动测试、浏览器模拟、真实平板表现、线上发布、研究与临床批准分别看待。

- 前端整套测试、类型检查、静态检查和构建通过；最后的收尾页说明修改后再次完成类型检查、静态检查与构建。
- 自动带练 API／服务层 276 项通过；患者与音频定向集 652 项通过；研究数据前端 38 项通过。新增覆盖包括终结反馈期间裁定、旧弹窗、部分跳过题评分、数据版本变化、音频系统中断与下载断流。
- Chrome 三条模拟走查通过：`adjudication-chains`、`recovery-chains`、`start-pause`。同时核对临时数据库账本；裁定走查增加确认框题号／环节和非法备注保留草稿的实际页面检查。
- 后端整套 `pytest` 与 `ruff` 通过，门禁退出码 0。运行过程有一条现有测试客户端依赖弃用提示，无测试失败。
- 差异格式检查通过。本轮未进行线上发布、真实老人／平板弱网试验、远端 CI、容器扫描、备份恢复演练；不将这些项目列为已通过。

复跑入口：`scripts/ci_gate.sh --only backend`、`scripts/ci_gate.sh --only frontend`，以及 `scripts/run-caregiver-demo20.sh --browser-check <上述场景>`。模拟服务只监听本机，正常退出会清理临时数据库。

### 继续实施后的验证（与上面的第一批区分）

- 前端整套测试、类型检查、静态检查与构建通过。
- 最后补充“曾经自动启动失败后，重置起点仍须手动启动”的准确说明；没有放宽自动重试。该次文案与状态呈现修改后，45 项定向测试、静态检查、构建及构建完整性检查再次通过。
- 指定起点 20 项接口回归通过；其中包括同键重试、不同起点／理由冲突、非法题号、权限、双要素逐环节跳过，以及从最后一题开始后正常结束场次。
- 恢复失败事实的质量读侧及相关接口定向验证通过；不接受缺少同一录音恢复证明的伪造重试标记。
- 独立复核修正了“恢复后第二次报不同错误码会把两次真实暂停漏计”的情况；最终质量读侧及接口 92 项回归通过，旧的无恢复证明重复失败仍不计为两次有效暂停。
- `answer-now` Chrome 模拟走查通过：实际点击停播，原生音频确实暂停且未触发播完；服务端保存打断回执，同题录音、上传、判分、反馈及安全暂停均通过，随后独立核对同一临时数据库。
- `start-position` Chrome 模拟走查通过：选择第 2 题、原因必选、取消及选回第 1 题不会意外自启、确认请求与首条命令题号一致；前一题只有具名跳过记录，没有伪造回答或音频。另以明确注入的状态回执检查“裁定已保存”页面呈现，这部分只证明界面；真实保存由 8 项后端故障回归验证。
- 原有 `adjudication-chains`、`recovery-chains` 与 `start-pause` Chrome 流程及同库账本回归通过。
- 恢复／自动带练 API、服务层与供应商故障裁定定向 295 项通过；覆盖旧识别／判分请求迟到成功和迟到失败四种并发情形。
- 提前回答与重复请求定向 89 项、处理任务定向 39 项通过；导出及研究读接口 143 项通过。
- 用真实 Alembic 旧版本及 HTTP 产生的模拟命令、回执、录音数据验证升级：数据逐值相同、外键检查通过、结构指纹与新版本一致。存在打断回执后回退被拒绝，版本与数据保持不变；迁移定向 2 项通过。
- 后端整套 `pytest` 与 `ruff` 门禁退出码 0；运行期间完成的最终增量（质量分轮次计数、打断提示需复核及迁移夹具）另分别通过上列 92／39／2 项定向回归。最后再次完成全仓静态检查与差异格式检查。
- 五条 Chrome 模拟流程均已通过：`start-position`、`answer-now`、`adjudication-chains`、`recovery-chains`、`start-pause`。最后的自动启动重置提示文案由定向测试、构建验证，不把此前浏览器运行冒充成这次文案修改后的再次走查。
- 本轮未部署、未推送，也未接触真实患者数据。正式升级前需要应用新迁移；本地通过不代表已经上线或真实平板／老人验证完成。

## 合并后的补查与统一发布候选

第一批修复 `21b23ff` 的 push 与 pull_request 两组 CI 均五项通过，正常受保护合并为 `163949c`（PR #20）。生产完整源码与独立 CI 构件清单仍核对为旧版 `3f49f32`，未提前切换。生产旧结构备份的隔离副本已完成候选迁移与真实应用启动演练；患者原库未在演练中改写。

补查又修正三处确认缺陷，统一纳入本次发布：

- **配对竞态：** 旧的自动重连在手工配对另一人后返回，可能覆盖新设备凭据或清除新绑定。现在绑定准确的配对轮次、旧凭据与患者绑定；迟到响应不得更改新状态。重复点配对只提交一次，绑定式配对不会沿用上一场的活动凭据，存储失败保留恢复所需数据。
- **人工题号：** 人工建题缺省序号原会成为空值。现在由该场次冻结题库核定题号，拒绝未知题目、题型或冲突序号；可核实冻结来源的历史空序号只在读侧补齐，不改旧数据与冻结研究快照。
- **跨场次续练：** 暂停的自动训练让出老人端后，原先不能直接接回。现在研究者点击继续时先安全激活已暂停场次。另一场仍在训练、热麦、处理或未完成收麦时拒绝切走；激活本身不启动 AI，仍须设备配对及原来的恢复许可。已有录音可以继续处理，不造新回答。

定向验证：配对实际请求竞态 45 项通过；人工题号相关 231 项及新增读侧/接口用例通过；跨场次激活与相关权限、现场切换、生命周期 317 项通过，新增 19 项另经独立复跑。之后以两个独立进程的真实请求复现旧状态覆盖竞态，并在激活读取权威状态前加 SQLite 写事务锁；新增交错用例验证另一场刚恢复时拒绝切走。

最终前端完整门禁通过；Chrome `recovery-chains` 与 `start-pause` 再次通过并核对同一临时库账本。恢复走查实际执行新增的激活再恢复请求；跨场次切换的权限、配对与保存录音闭环由接口测试验证。最终后端完整门禁、远端 CI 与发布状态随后登记，不把局部通过当作已经部署。

完整本地后端门禁随后通过（`ruff`、整套 `pytest`，退出码 0）；最终 SQLite 同步保护及独立进程竞态另有 20 项定向和独立复测通过。

最终浏览器补跑发现验收工具中的两处旧合同：一处把提前回答应有的复核标记当成失败，另一处误用仅供旧协议恢复的判类规则。保留同一临时数据库核实回答实际已完成、识别及判分无错误、复核标记与交互事件一致；生产现代恢复、推进和导出校验没有同类误拒。验收工具改为直接复用现代完成校验，并保留冻结内容、识别顺序、规则判类、真实复核交互和提示打断证据核对；普通完整播放路径仍要求原来的无复核标记。失败日志和合成库保留，不通过改写数据库让验收变绿。

修正后的验收工具完成相关 53 项测试，独立复核其中 44 项通过；对原失败合成库重新完整核账通过且数据库 SHA-256 未改变。另起全新 Chrome `answer-now` 流程及同库核账也通过。最终五条浏览器流程全部通过：`start-position`、`answer-now`、`adjudication-chains`、`recovery-chains`、`start-pause`。这次补丁仅修改验收工具和测试，产品代码与前端构建未再改变；最终云端门禁和实际发布仍另记。

## 实际发布回执（2026-09-24）

应用 `ce314df5320d2af92318bee54bf7080500c61c1b` 经 [PR #21](https://github.com/EricXu-0805/nmu-ad-language-training/pull/21) 正常合并为 `bdd8142b980f829a84cb7ea012dfbfe35824e1fe`；push CI `35982579477`、PR CI `35982583824` 各五项成功。应用于 10:32:56 UTC 启动，10:38:05 UTC 重新开放；线上 830 个源码文件和 19 个网页构件与候选及 CI 产物一致，完整预检 9/9 通过。重新开放后的公网接口检查通过，真实 IAB 匿名首页和登录表单已观察通过；这不等于认证后训练或真实设备验收。

迁移 `f4b2d8c1a635` → `a7c3e9d2b641` 后逐表核对原有内容一致，完整旧树及独立数据库、录音副本保留。迁移前 `20260924-102957`、换版后 `20260924-103352` 快照均完成服务器与 Mac 异地一致性校验；Mac 同步重跑无新增、无冲突，3 份旧 daily 快照原样移存，原 12:30 计划及服务器六个运维定时器恢复。容量、系统安全更新和旧日志保留的实际结果见 [部署状态记录](RELEASE_STATE.md)，完整私有证据位于 `审核报告_20260924/final-ce314df/`。重新开放后禁止自动回滚旧库；原临床阈值、单次回答评分规则、对照组内容及首页试验版限制不变，真实老人、平板和弱网仍待现场验收。

## 继续迭代后的实际发布（2026-09-24 20:38 UTC）

`af0c32e` 已上线，含后台播报／录音生命周期、结束记录保存恢复、研究数据来源与量表入口、多进程启动／暂停、管理员监督锁冲突修复，以及备份轮转容量预测纠正。push／PR 两套 CI 各五项成功，实际部署 833 个源码文件和 19 个网页构件核对一致，预检 9/9；前后备份、Mac 副本、隔离恢复与公网核对均完成。库头保持 `a7c3e9d2b641`，无结构迁移。

VPS 清理保留了必要备份及经过恢复验证的双份历史归档；发布后可用约 6.82GiB，当前无需为了本次发布扩容。旧容量算法忽略轮转的判断已纠正，新的 30 天预测仅覆盖每日备份，不能当作整机寿命。实际时间、提交、证据及运行边界见 [部署状态记录](RELEASE_STATE.md)。真实平板与现场弱网尚未验收。
